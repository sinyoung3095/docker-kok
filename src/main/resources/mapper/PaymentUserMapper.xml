<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.kok.mapper.PaymentUserMapper">
    <insert id="insertPaymentUser">
        insert into tbl_payment_user (
            payment_id,
            user_id,
            advertisement_id
        )
        values (
                   #{paymentId},
                   #{userId},
                   #{advertisementId}
               )
    </insert>
    <insert id="insertPaymentUserByMember">
        insert into tbl_payment_user(payment_id, user_id, request_experience_id)
        values (#{paymentId}, #{userId}, #{requestExperienceId})
    </insert>
</mapper>

